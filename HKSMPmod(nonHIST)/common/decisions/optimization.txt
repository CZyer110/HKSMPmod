optimization = {
	
	HKS_na_optimization_fucker = { # хуета в северной америке для оптимизации
		fire_only_once = yes
		priority = 100 # боту не похуй?
		ai_will_do = {
		base = 100.0
		}
		
		visible = {
			
			MEX = { is_ai = yes }
			ELS = { is_ai = yes }
			HON = { is_ai = yes }
			NIC = { is_ai = yes }
			COS = { is_ai = yes }
			PAN	= { is_ai = yes }
			GUA = { is_ai = yes }
			HAI = { is_ai = yes }
			DOM = { is_ai = yes }
			CUB = { is_ai = yes }
			COL = { is_ai = yes } # НУ А ВДРУГ кто то сядет на мексику/колумбию etc. 
			
		}
		
		available = {
			BRA = { is_ai = yes }
			MEX = { is_ai = yes }
			ELS = { is_ai = yes }
			HON = { is_ai = yes }
			NIC = { is_ai = yes }
			COS = { is_ai = yes }
			PAN	= { is_ai = yes }
			GUA = { is_ai = yes }
		}
		
		complete_effect = {
			ROOT = { 
				annex_country = { 
					target = PAN 
					transfer_troops = yes 
				}
				annex_country = { 
					target = COS 
					transfer_troops = yes 
				}
				annex_country = { 
					target = HON 
					transfer_troops = yes 
				}
				annex_country = { 
					target = ELS 
					transfer_troops = yes 
				}
				annex_country = { 
					target = GUA 
					transfer_troops = yes 
				}
				annex_country = { 
					target = HAI 
					transfer_troops = yes 
				}
				annex_country = { 
					target = DOM 
					transfer_troops = yes 
				}
			}
		}
	
	
}